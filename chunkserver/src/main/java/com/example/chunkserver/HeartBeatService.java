package com.example.chunkserver;

public class HeartBeatService {
    //send heartbeats with current state of chunkstorage to master
    //on every heartbeat timer-every 5/10 secs, and on every write request received
}
